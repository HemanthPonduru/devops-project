name: CI Pipeline

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Docker
        uses: docker/setup-buildx-action@v3

      - name: Build Docker image
        run: docker build -t devops-backend ./backend

      - name: Login to Docker Hub
        run: echo "${{ secrets.Hemanth@1750 }}" | docker login -u "${{ secrets.hemanthponduru }}" --password-stdin

      - name: Tag Docker image
        run: docker tag devops-backend ${{ secrets.hemanthponduru }}/devops-backend:latest

      - name: Push Docker image
        run: docker push ${{ secrets.hemanthponduru }}/devops-backend:latest